quarkus:
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    jdbc:
      url: ${DB_URL}
    db-kind: postgresql
  http:
    port: 7081
  flyway:
    migrate-at-start: true
    out-of-order: true